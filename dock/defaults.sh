# hide the dock unless mousing over
defaults write com.apple.dock "autohide" -bool "true"

# set dock size to largest possible (optimize for ease of clicking)
defaults write com.apple.dock "tilesize" -int "128"