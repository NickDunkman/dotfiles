# holding down a key should repeatedly press that key
# (instead of accents menu)
#defaults write NSGlobalDomain "ApplePressAndHoldEnabled" -bool "false"
# ^ disabling this for now so that I can use the accents menu for French stuff!